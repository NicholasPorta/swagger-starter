# Attivare Swagger in 1 minuto (Spring Boot)

## Passaggi

### 1. Aggiungi le dipendenze nel `pom.xml`

### 2. Crea la classe `OpenAPIConfig.java`

##  Accesso a Swagger

Una volta avviata l'app:
👉 `http://localhost:8080/swagger-ui.html`
